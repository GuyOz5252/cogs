<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <Description>Optional compatibility layer that translates legacy Newtonsoft JSON pipeline configs into the modern CoGS pipeline model.</Description>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Newtonsoft.Json" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\CoGS.Core\CoGS.Core.csproj" />
    </ItemGroup>

</Project>
